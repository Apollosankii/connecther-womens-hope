import { LightColors } from '@/theme/palette';

/**
 * Light-theme snapshot for rare non-React contexts only.
 * Screens and UI must use `useTheme().colors` from `@/providers/ThemeProvider` so dark mode works.
 */
export const Colors = LightColors;

