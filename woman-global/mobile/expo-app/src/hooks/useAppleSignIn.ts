/**
 * TypeScript resolves this file. At bundle time, Metro prefers `useAppleSignIn.native.ts`
 * (iOS) or `useAppleSignIn.web.ts` (web) instead of this shim.
 */
export { useAppleSignIn } from './useAppleSignIn.native';

